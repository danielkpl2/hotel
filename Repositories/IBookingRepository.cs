using Hotel.Models;

namespace Hotel.Repositories;

public interface IBookingRepository
{
    // Task<Booking?> GetByReferenceAsync(string bookingReference);
    // Task<List<Booking>> GetConflictingBookingsAsync(List<int> roomIds, DateOnly checkInDate, DateOnly checkOutDate);
    // Task<Booking> CreateAsync(Booking booking);
    // Task<bool> HasConflictingBookingAsync(int roomId, DateOnly checkInDate, DateOnly checkOutDate);
}